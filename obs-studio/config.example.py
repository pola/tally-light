# HTTP/HTTPS server where ../www-server/index.js runs
server = 'https://...'

# path to Python libraries (can be None)
python_lib_path = '.../lib/python.../site-packages'

# empty list means that changes to all sources are reported
source_types = []

# a non-empty list means that only changes to those sources are reported
# source_types = ['window_capture']